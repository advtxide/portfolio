---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className }: Props = Astro.props;
---

<div class={cn("px-2.5 py-6 lg:py-12", className ? className : "")}>
  <slot />
</div>
