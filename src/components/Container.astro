---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className }: Props = Astro.props;
---

<main class={cn("px-6 py-8 lg:py-6", className ? className : "")}>
  <slot />
</main>
