---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className }: Props = Astro.props;
---

<main class={cn("mx-auto px-4 py-8 lg:p-6", className ? className : "")}>
  <slot />
</main>
