---
import { cn } from "@/lib/utils";

interface Props {
  className?: string;
}

const { className }: Props = Astro.props;
---

<main class={cn("p-6", className ? className : "")}>
  <slot />
</main>
